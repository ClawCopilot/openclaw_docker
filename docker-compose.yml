# 公共配置锚点
x-base-service:
  &base-service
  build:
    context: .
    args:
      - BASE_IMAGE=${BASE_IMAGE:-ghcr.m.daocloud.io/openclaw/openclaw:latest}
      - CONTAINER_HOME=${CONTAINER_HOME:-/home/node}
      - TZ=${TZ:-Asia/Shanghai}
      - PIP_MIRROR=${PIP_MIRROR:-tuna}
      - RUST_VERSION=${RUST_VERSION:-stable}
      - RUSTUP_MIRROR=${RUSTUP_MIRROR:-tuna}
      - RUST_CRATES_MIRROR=${RUST_CRATES_MIRROR:-tuna}
      - GO_VERSION=${GO_VERSION:-1.25.8}
      - GOPROXY_MIRRORS=${GOPROXY_MIRRORS:-goproxy.cn,goproxy.io,direct}
      - DOCKER_HUB_MIRRORS=${DOCKER_HUB_MIRRORS:-daocloud,aliyun,tuna}
      - OPENCLAW_VERSION=${OPENCLAW_VERSION:-latest}
      - INSTALL_DOCKER=${INSTALL_DOCKER:-false}
      - INSTALL_PODMAN=${INSTALL_PODMAN:-true}
      - INSTALL_DOCKER_COMPOSE=${INSTALL_DOCKER_COMPOSE:-false}
      - DOCKER_COMPOSE_VERSION=${DOCKER_COMPOSE_VERSION:-latest}
  environment:
    - PORT=18789
    - NODE_ENV=${OPENCLAW_NODE_ENV:-production}
    - npm_config_registry=${npm_config_registry:-https://registry.npmmirror.com/}
    - pnpm_config_registry=${pnpm_config_registry:-https://registry.npmmirror.com/}
    - TZ=${TZ:-Asia/Shanghai}
    - OPENCLAW_VERSION=${OPENCLAW_VERSION:-latest}
  restart: ${CONTAINER_RESTART_POLICY:-unless-stopped}
  logging:
    driver: json-file
    options:
      max-size: "${LOG_MAX_SIZE:-10m}"
      max-file: "${LOG_MAX_FILE:-3}"
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:18789/health"]
    interval: ${HEALTHCHECK_INTERVAL:-30s}
    timeout: ${HEALTHCHECK_TIMEOUT:-10s}
    start_period: ${HEALTHCHECK_START_PERIOD:-5s}
    retries: ${HEALTHCHECK_RETRIES:-3}
  privileged: true
  network_mode: ${NETWORK_MODE:-bridge}

# 服务配置
services:
  # 网关 1 - serv
  serv:
    <<: *base-service
    container_name: serv
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 8g
    volumes:
      - ./serv/supervisor/conf.d:/etc/supervisor/conf.d     # Supervisor config
      - ./serv/.ssh:/home/node/.ssh:U,z                    # Config ssh
      - ./serv/.openclaw:/home/node/.openclaw:U,z          # Config and data
      - ./serv/workspace:/home/node/workspace:U,z          # Agent workspace
      - ./serv/apps:/home/node/apps:U,z                    # Config apps
      - ./share:/home/node/share:U,z                              # Config share
    environment:
      - GATEWAY_ID=serv

  # 网关 2 - coder1
  coder1:
    <<: *base-service
    container_name: coder1
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 8g
    volumes:
      - ./coder1/supervisor/conf.d:/etc/supervisor/conf.d     # Supervisor config
      - ./coder1/.ssh:/home/node/.ssh:U,z                    # Config ssh
      - ./coder1/.openclaw:/home/node/.openclaw:U,z          # Config and data
      - ./coder1/workspace:/home/node/workspace:U,z          # Agent workspace
      - ./coder1/apps:/home/node/apps:U,z                    # Config apps
      - ./share:/home/node/share:U,z                              # Config share
    environment:
      - GATEWAY_ID=coder1

  # 网关 3 - coder2
  coder2:
    <<: *base-service
    container_name: coder2
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 8g
    volumes:
      - ./coder2/supervisor/conf.d:/etc/supervisor/conf.d     # Supervisor config
      - ./coder2/.ssh:/home/node/.ssh:U,z                    # Config ssh
      - ./coder2/.openclaw:/home/node/.openclaw:U,z          # Config and data
      - ./coder2/workspace:/home/node/workspace:U,z          # Agent workspace
      - ./coder2/apps:/home/node/apps:U,z                    # Config apps
      - ./share:/home/node/share:U,z                              # Config share
    environment:
      - GATEWAY_ID=coder2

  # 网关 4 - coder3
  coder3:
    <<: *base-service
    container_name: coder3
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 8g
    volumes:
      - ./coder3/supervisor/conf.d:/etc/supervisor/conf.d     # Supervisor config
      - ./coder3/.ssh:/home/node/.ssh:U,z                    # Config ssh
      - ./coder3/.openclaw:/home/node/.openclaw:U,z          # Config and data
      - ./coder3/workspace:/home/node/workspace:U,z          # Agent workspace
      - ./coder3/apps:/home/node/apps:U,z                    # Config apps
      - ./share:/home/node/share:U,z                              # Config share
    environment:
      - GATEWAY_ID=coder3

